import { BtnLoadDirective } from './btn-load.directive';

describe('BtnLoadDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnLoadDirective();
    expect(directive).toBeTruthy();
  });
});
